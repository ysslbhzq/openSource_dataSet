Delete the thermometer device certificate Certificates3 and the certificate coupled with the private key / public key pair. If the device does not support TLS or IEEE 802.1X, an error message is returned without deleting any certificates.
