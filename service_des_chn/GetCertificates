为了进行TLS身份验证，此操作将获得所有设备服务器证书（包括自签名证书），并为IEEE 802.1X身份验证的目的获取所有设备客户端证书。该命令仅列出设备的TLS服务器证书和IEEE 802.1X客户端证书（既不信任CA证书也不信任根证书）。证书作为二进制数据返回。支持TLS的设备应支持该命令，证书应使用ASN.1 [X.681]，[X.682]，[X.683] DER [X.690]编码规则进行编码。
